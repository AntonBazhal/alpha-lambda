machine:
  node:
    version: 4.6.0

test:
  post:
    - npm install -g coveralls
    - cat ./coverage/lcov.info | coveralls

deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    owner: AntonBazhal
    commands:
      - npm publish
